# 将Django+MySQL+Vue+MP3方言搜索应用打包为Electron桌面应用 - 详细配置指南

下面我将详细标注每个步骤中需要修改为您自己项目路径或名称的地方。所有需要替换的内容都会用**【需修改】**标记。

## 一、调整后端架构

### 1. 数据库迁移脚本

```python
# migrate_db.py
import sqlite3
import pandas as pd
import pymysql
import os

def migrate_from_mysql_to_sqlite():
    # 连接MySQL
    mysql_conn = pymysql.connect(
        host='localhost',                      # 【需修改】替换为您的MySQL主机地址
        user='your_mysql_user',                # 【需修改】替换为您的MySQL用户名
        password='your_mysql_password',        # 【需修改】替换为您的MySQL密码
        database='your_database'               # 【需修改】替换为您的MySQL数据库名
    )
    
    # 创建SQLite数据库
    sqlite_conn = sqlite3.connect('db.sqlite3')
    
    # 获取MySQL表结构
    mysql_cursor = mysql_conn.cursor()
    mysql_cursor.execute("SHOW TABLES")
    tables = mysql_cursor.fetchall()
    
    # 迁移每个表
    for table in tables:
        table_name = table[0]
        print(f"迁移表: {table_name}")
        
        # 读取MySQL表数据
        query = f"SELECT * FROM {table_name}"
        df = pd.read_sql(query, mysql_conn)
        
        # 写入SQLite
        df.to_sql(table_name, sqlite_conn, if_exists='replace', index=False)
    
    mysql_conn.close()
    sqlite_conn.close()
    
    print("数据迁移完成")

if __name__ == "__main__":
    migrate_from_mysql_to_sqlite()
```

### 2. 修改Django设置

```python
# settings.py
import os
import sys

# 检测是否在打包环境中
if getattr(sys, 'frozen', False):
    # 在PyInstaller/Electron环境中
    BASE_DIR = os.path.dirname(sys.executable)
    # 在Windows上，使用用户数据目录存储数据
    if sys.platform == 'win32':
        import appdirs
        user_data_dir = appdirs.user_data_dir(
            "DialectApp",                       # 【需修改】替换为您的应用名称
            "YourCompany"                       # 【需修改】替换为您的公司名称
        )
        os.makedirs(user_data_dir, exist_ok=True)
        
        DATABASES = {
            'default': {
                'ENGINE': 'django.db.backends.sqlite3',
                'NAME': os.path.join(user_data_dir, 'db.sqlite3'),
            }
        }
        MEDIA_ROOT = os.path.join(user_data_dir, 'media')
    else:
        MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
else:
    # 在正常Python环境中
    BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
    MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

STATIC_URL = '/static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'static')

MEDIA_URL = '/media/'
```

### 3. Django独立启动脚本

```python
# run_server.py
import os
import sys
import django
from django.core.management import call_command
import signal
import threading
import time

# 设置Django环境
os.environ.setdefault(
    'DJANGO_SETTINGS_MODULE', 
    'your_project.settings'                    # 【需修改】替换为您的Django项目的settings模块路径
)                                              # 例如: 'dialect_project.settings'

def run_django():
    # 初始化Django
    django.setup()
    
    # 执行数据库迁移
    call_command('migrate')
    
    # 启动Django开发服务器
    call_command('runserver', '127.0.0.1:8000', '--noreload')

if __name__ == "__main__":
    # 在单独线程中启动Django
    django_thread = threading.Thread(target=run_django)
    django_thread.daemon = True
    django_thread.start()
    
    # 保持脚本运行
    try:
        while True:
            time.sleep(1)
    except KeyboardInterrupt:
        sys.exit(0)
```

## 二、配置Electron应用

### 1. 创建Electron项目结构

创建以下目录结构：

```
dialect-electron-app/             # 【需修改】可以替换为您想要的项目文件夹名称
```

### 2. 创建主进程文件 main.js

```javascript
const { app, BrowserWindow, ipcMain, dialog } = require('electron');
const path = require('path');
const { spawn } = require('child_process');
const fs = require('fs-extra');
const isDev = require('electron-is-dev');

let mainWindow;
let djangoProcess = null;

// 获取Python可执行文件路径
function getPythonPath() {
  if (isDev) {
    return 'python'; // 开发环境
  } else {
    // 生产环境使用打包的Python
    if (process.platform === 'win32') {
      return path.join(process.resourcesPath, 'python', 'python.exe');
    } else {
      return path.join(process.resourcesPath, 'python', 'bin', 'python');
    }
  }
}

// 启动Django后端
async function startDjangoServer() {
  const pythonPath = getPythonPath();
  
  // 获取Django项目路径
  const djangoPath = isDev 
    ? path.join(__dirname, 'backend')          // 【需修改】如果您的后端目录不是'backend'，请修改
    : path.join(process.resourcesPath, 'backend');
  
  console.log('启动Django服务器...');
  console.log(`Python路径: ${pythonPath}`);
  console.log(`Django项目路径: ${djangoPath}`);
  
  // 运行Django服务
  const djangoScript = path.join(djangoPath, 'run_server.py');
  
  // 在Windows上设置shell: true避免窗口闪现
  djangoProcess = spawn(pythonPath, [djangoScript], {
    cwd: djangoPath,
    shell: process.platform === 'win32'
  });
  
  // 日志处理
  djangoProcess.stdout.on('data', (data) => {
    console.log(`Django: ${data}`);
  });
  
  djangoProcess.stderr.on('data', (data) => {
    console.error(`Django错误: ${data}`);
  });
  
  // 等待Django启动
  return new Promise((resolve) => {
    setTimeout(resolve, 3000);
  });
}

// 创建主窗口
async function createWindow() {
  // 先启动Django
  await startDjangoServer();
  
  mainWindow = new BrowserWindow({
    width: 1200,                               // 【需修改】可以根据需要调整窗口大小
    height: 800,
    webPreferences: {
      nodeIntegration: false,
      contextIsolation: true,
      preload: path.join(__dirname, 'preload.js')
    }
  });
  
  // 加载前端
  const startUrl = isDev
    ? 'http://localhost:8080'                  // 【需修改】如果您的Vue开发服务器端口不是8080，请修改
    : `http://localhost:8000`;
  
  await mainWindow.loadURL(startUrl);
  
  if (isDev) {
    mainWindow.webContents.openDevTools();
  }
  
  mainWindow.on('closed', () => {
    mainWindow = null;
  });
}

// 处理App生命周期
app.whenReady().then(createWindow);

// 处理Excel文件导入
ipcMain.handle('select-excel-file', async () => {
  const result = await dialog.showOpenDialog({
    properties: ['openFile'],
    filters: [{ name: 'Excel Files', extensions: ['xlsx', 'xls'] }]
  });
  
  if (!result.canceled && result.filePaths.length > 0) {
    return result.filePaths[0];
  }
  return null;
});

// 应用退出时清理
app.on('will-quit', () => {
  if (djangoProcess) {
    if (process.platform === 'win32') {
      // Windows上需要终止进程树
      spawn('taskkill', ['/pid', djangoProcess.pid, '/f', '/t']);
    } else {
      djangoProcess.kill('SIGTERM');
    }
  }
});

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') {
    app.quit();
  }
});

app.on('activate', () => {
  if (BrowserWindow.getAllWindows().length === 0) {
    createWindow();
  }
});
```

### 4. 修改package.json

```json
{
  "name": "dialect-app",                       // 【需修改】替换为您的应用名称
  "version": "1.0.0",
  "description": "方言搜索应用",                // 【需修改】替换为您的应用描述
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build",
    "build-win": "electron-builder build --win",
    "build-mac": "electron-builder build --mac"
  },
  "build": {
    "appId": "com.yourcompany.dialect-app",    // 【需修改】替换为您的应用ID
    "productName": "方言搜索",                  // 【需修改】替换为您的产品名称
    "files": [
      "main.js",
      "preload.js",
      "build/**/*",                            // 【需修改】如果Vue构建输出目录不是build，请修改
      "node_modules/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "backend",                     // 【需修改】如果后端目录不是backend，请修改
        "to": "backend"
      },
      {
        "from": "python-env",                  // 【需修改】如果Python环境目录不是python-env，请修改
        "to": "python"
      }
    ],
    "directories": {
      "buildResources": "resources",
      "output": "dist"
    },
    "win": {
      "target": ["nsis", "portable"]
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": "AppImage"
    }
  },
  "devDependencies": {
    "electron": "^20.1.0",
    "electron-builder": "^23.3.3",
    "electron-is-dev": "^2.0.0"
  },
  "dependencies": {
    "fs-extra": "^10.1.0"
  }
}
```

## 三、调整Vue前端

### 1. 修改Vue前端API调用

```javascript
// src/api/index.js
const baseUrl = process.env.NODE_ENV === 'production'
  ? 'http://localhost:8000/api'                // 【需修改】如果您的Django API路径不是/api，请修改
  : '/api';  // 开发环境（使用代理）

export const searchDialects = async (searchTerm) => {
  const response = await fetch(`${baseUrl}/search/?q=${encodeURIComponent(searchTerm)}`);
                                                // 【需修改】替换为您实际的搜索API端点
  return response.json();
};

export const getAudioUrl = (audioFile) => {
  if (!audioFile) return '';
  return process.env.NODE_ENV === 'production'
    ? `http://localhost:8000${audioFile}`      // 【需修改】确保此处的路径与Django的MEDIA_URL一致
    : audioFile;  // 开发环境
};
```

### 2. 配置Vue开发环境

```javascript
// vue.config.js
module.exports = {
  devServer: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',       // 【需修改】替换为您的Django开发服务器地址
        changeOrigin: true
      },
      '/media': {
        target: 'http://localhost:8000',       // 【需修改】替换为您的Django开发服务器地址
        changeOrigin: true
      }
    }
  },
  // 打包配置
  outputDir: '../build'                        // 【需修改】替换为Electron项目中期望的前端构建输出目录
}
```

## 四、处理Python环境

### 1. 创建Python虚拟环境

```bash
python -m venv python-env                       # 【需修改】可以替换为您想要的环境目录名
source python-env/bin/activate  # Mac/Linux
# 或 python-env\Scripts\activate  # Windows

# 安装依赖 - 确保包含您项目中用到的所有包
pip install django djangorestframework pandas openpyxl pymysql appdirs
                                                # 【需修改】添加您项目中使用的其他依赖包
pip freeze > requirements.txt
```

### 2. 在Django中添加Excel上传处理

```python
# views.py
from django.http import JsonResponse
from rest_framework.decorators import api_view
from .models import DialectWord                 # 【需修改】替换为您的模型名称
import pandas as pd
import os

@api_view(['POST'])
def upload_excel(request):
    if 'file' not in request.FILES:
        return JsonResponse({'error': '未上传文件'}, status=400)
    
    excel_file = request.FILES['file']
    
    # 检查文件扩展名
    if not excel_file.name.endswith('.xlsx') and not excel_file.name.endswith('.xls'):
        return JsonResponse({'error': '请上传Excel文件(.xlsx或.xls)'}, status=400)
    
    try:
        # 读取Excel文件
        df = pd.read_excel(excel_file)
        
        # 检查必要列
        required_columns = ['word', 'dialect_type', 'audio_file']
                                                # 【需修改】替换为您的Excel文件中的必要列名
        missing_columns = [col for col in required_columns if col not in df.columns]
        
        if missing_columns:
            return JsonResponse({
                'error': f'Excel缺少必要列: {", ".join(missing_columns)}'
            }, status=400)
        
        # 导入数据
        count = 0
        for _, row in df.iterrows():
            word = row['word']                  # 【需修改】替换为您的Excel列名
            dialect_type = row['dialect_type']  # 【需修改】替换为您的Excel列名
            audio_file = row['audio_file'] if 'audio_file' in row else None
                                                # 【需修改】替换为您的Excel列名
            phonetic = row['phonetic'] if 'phonetic' in row else ''
                                                # 【需修改】替换为您的Excel列名
            
            # 创建或更新词条
            obj, created = DialectWord.objects.update_or_create(
                word=word,
                dialect_type=dialect_type,
                defaults={
                    'phonetic': phonetic,
                    'audio_file': audio_file
                }
            )
            
            count += 1
        
        return JsonResponse({'status': 'success', 'count': count})
    
    except Exception as e:
        return JsonResponse({'error': str(e)}, status=500)
```

### 3. 确保在urls.py中添加路由

```python
# urls.py
from django.urls import path
from . import views

urlpatterns = [
    # 其他URL路由...
    path('api/upload-excel/', views.upload_excel, name='upload-excel'),
                                                # 【需修改】确保URL路径与前端请求匹配
    path('api/search/', views.search_dialects, name='search-dialects'),
                                                # 【需修改】确保URL路径与前端请求匹配
]
```

## 五、模型定义

```python
# models.py
from django.db import models

class DialectWord(models.Model):                # 【需修改】替换为您的模型名称
    word = models.CharField(max_length=100)     # 【需修改】根据您的数据调整字段
    phonetic = models.CharField(max_length=100, blank=True)
                                                # 【需修改】根据您的数据调整字段
    dialect_type = models.CharField(max_length=50)
                                                # 【需修改】根据您的数据调整字段
    audio_file = models.FileField(upload_to='dialect_audio', blank=True, null=True)
                                                # 【需修改】替换为您的音频文件存储路径
    
    def __str__(self):
        return self.word
```

## 六、构建与打包

### 1. 构建Vue前端

```bash
# 进入Vue项目目录
cd frontend                                      # 【需修改】替换为您的Vue项目目录

# 构建前端
npm run build
```

### 2. 准备Django后端

```bash
# 复制Django项目到Electron项目的backend目录
cp -r /your/django/project/* backend/           # 【需修改】替换为您的Django项目路径

# 确保添加run_server.py脚本
cp run_server.py backend/
```

### 3. 打包Electron应用

```bash
# 回到Electron项目目录
cd dialect-electron-app                         # 【需修改】替换为您的Electron项目目录

# 打包应用
npm run build-win  # Windows
# 或 npm run build-mac  # Mac
```

## 七、目录结构总览

最终的项目目录结构应该如下：

```
dialect-electron-app/                          # 【需修改】您的Electron项目根目录
├── main.js                                    # Electron主进程文件
├── preload.js                                 # Electron预加载脚本
├── package.json                               # Electron项目配置
├── node_modules/                              # Node.js依赖
├── build/                                     # 【需修改】Vue前端构建输出目录
│   ├── index.html
│   ├── js/
│   └── css/
├── backend/                                   # 【需修改】Django后端目录
│   ├── manage.py
│   ├── run_server.py                          # Django启动脚本
│   ├── db.sqlite3                             # SQLite数据库
│   ├── your_project/                          # 【需修改】您的Django项目目录
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   └── your_app/                              # 【需修改】您的Django应用目录
│       ├── models.py
│       ├── views.py
│       ├── urls.py
│       └── migrations/
├── python-env/                                # 【需修改】Python虚拟环境目录
└── dist/                                      # 打包输出目录
```

## 注意事项

1. 所有标记了【需修改】的地方都需要根据您实际的项目结构和命名来调整。
2. 确保您的Django项目中的模型字段名称与Excel文件中的列名匹配，或者在导入时做相应的映射。
3. 音频文件路径在从MySQL迁移到SQLite后可能需要调整，确保它们指向正确的位置。
4. 在打包前，请务必在开发环境中测试所有功能，特别是Excel导入和音频播放功能。
5. 如果您的Django项目依赖其他Python包，请确保将它们添加到虚拟环境中。

按照以上指南调整您的代码和配置，您就能成功将Django+MySQL+Vue+MP3方言搜索应用打包为Electron桌面应用，用户只需双击可执行文件即可使用全部功能。